#include<iostream>
#include<string.h>
using namespace std;

int main()
{
    int n, c = 0, i, ii = 0;
    char player[100];

    cin >> player;
    n = strlen(player);

    if((n >= 2) && (n <= 100))
    {
        for(i = 0; i < n; i++)
        {
            if(player[i])
        }
    }

    return 0;
}
